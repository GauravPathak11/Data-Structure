#include<stdio.h>
void cofactor(int mat[n][n],int p,int q,int n){
    int i=0,j=0;
    for (int row = 0; row < n; row++)
    {
        for (int col = 0; col < n; col++)
        {
            //  Copying into temporary matrix only those
            //  element which are not in given row and
            //  column
}
int main(){

    return 0;
}